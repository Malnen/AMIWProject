import { RefereeRolePipe } from './referee-role.pipe';

describe('RefereeRolePipe', () => {
  it('create an instance', () => {
    const pipe = new RefereeRolePipe();
    expect(pipe).toBeTruthy();
  });
});
