import { LastGamesPipe } from './last-games.pipe';

describe('LastGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new LastGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
